There's no egg/dist for this yet. It's easy enough to drop the module dir
into your project for now if you're interested in the modules.

However if you think it's in an installable state and want it early patches
are always welcome.
